// ---------------------------------------------------------------------------
// core/icons.js — initialize Lucide icons declared via `<i data-lucide="...">`.
// ---------------------------------------------------------------------------

import { createIcons, icons } from "lucide";

export function init() {
  createIcons({ icons });
}
